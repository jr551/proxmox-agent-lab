Remote programs installed onto the lab host or guests, kept out of the
Python modules so they read as the shell/Python they are. Loaded once at
import time by the owning module; do not edit the loaded constants.
